# 真假药材分析系统数据字典（MVP 正式版）

| 属性 | 值 |
|---|---|
| 文档版本 | 1.2 |
| 文档状态 | 正式版 |
| 更新日期 | 2026-08-23 |
| 适用范围 | 用户认证、账号安全与注销、用户资料、图片上传、异步鉴别、药材库、历史记录、投诉反馈 |
| 不含范围 | 社区、养生堂、挑战赛、运营后台 |

## 1. 总体约定

- 数据库使用 MySQL 8.0，字符集 `utf8mb4`。
- 主键使用 `BIGINT UNSIGNED`，由服务端生成。
- 时间字段使用 `DATETIME(3)`，数据库统一存储 UTC；接口使用 ISO 8601 UTC 返回。
- 所有业务表包含 `created_at`、`updated_at`；软删除表额外包含 `deleted_at`。
- 密码仅保存 Argon2id 哈希；验证码仅保存哈希，绝不保存明文。
- 图片存对象存储，数据库保存对象键、元数据和访问控制信息。
- 枚举值统一使用小写英文，数据库和 API 必须保持一致。

## 2. 枚举定义

### 2.1 鉴别任务状态 `identification_status`

| 值 | 含义 | 是否终态 |
|---|---|:---:|
| `created` | 已创建，等待文件校验 | 否 |
| `queued` | 已入队，等待模型服务 | 否 |
| `processing` | 模型分析中 | 否 |
| `succeeded` | 已形成可展示结果 | 是 |
| `uncertain` | 分析完成但无法形成可靠结论 | 是 |
| `failed` | 处理失败 | 是 |
| `cancelled` | 用户或系统取消 | 是 |

允许的状态流转：`created -> queued -> processing -> succeeded|uncertain|failed`；`created|queued -> cancelled`。

任务状态与结论是两个独立维度：状态描述任务生命周期的终态，结论描述分析结论。`succeeded` 状态下结论为 `authentic`、`counterfeit` 或 `unrecognized`；`uncertain` 状态的结论固定为 `uncertain`。`unrecognized` 已形成可展示的结果页（明确提示无法识别），因此复用 `succeeded` 状态；`uncertain` 单独作为任务状态，是为了在列表筛选、统计和展示约束上与正常完成区分。

### 2.2 真伪结论 `authenticity_conclusion`

`authentic` 真品倾向；`counterfeit` 假品倾向；`uncertain` 无法可靠判断；`unrecognized` 无法识别药材。

`uncertain` 和 `unrecognized` 不得展示为确定的真品或假品。

### 2.3 鉴别任务失败原因 `identification_failure_code`

仅 `identification_request.status = failed` 时写入，其余状态为 NULL。该编码描述异步任务的终态失败原因，独立于 API 统一错误码，长期落库保存。

| 值 | 含义 |
|---|---|
| `image_unreadable` | 图片无法解码或无法用于鉴别 |
| `model_service_unavailable` | 模型服务不可用 |
| `model_timeout` | 模型分析超时 |
| `model_internal_error` | 模型服务内部错误 |
| `invalid_model_response` | 模型回调结果校验不通过（如非法结论或置信度） |
| `internal_error` | 服务端内部错误，兜底 |

`uncertain`、`unrecognized` 属于分析结论而非任务失败，`cancelled` 为独立终态，均不写入本字段。写入方约定：`image_unreadable`、`model_timeout`、`model_internal_error` 由模型回调或主服务超时看门狗写入，回调携带的失败码以此三者为限；`model_service_unavailable`、`invalid_model_response`、`internal_error` 由主服务写入。与机器学习服务对齐后可扩展新值，但不得复用 API 错误码。

## 3. 数据表

### 3.1 `user_account` 用户账户

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 用户 ID |
| nickname | VARCHAR(64) | 否 | | | 昵称 |
| avatar_asset_id | BIGINT UNSIGNED | 是 | NULL | FK `media_asset.id` | 头像 |
| status | VARCHAR(16) | 否 | `active` | IDX | `active`、`disabled`、`deleted` |
| last_login_at | DATETIME(3) | 是 | NULL | | 最近登录时间 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |
| deleted_at | DATETIME(3) | 是 | NULL | IDX | 软删除时间 |

### 3.2 `user_identity` 登录身份

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 身份 ID |
| user_id | BIGINT UNSIGNED | 否 | | FK，IDX | 所属用户 |
| provider | VARCHAR(16) | 否 | | UK(provider, provider_user_id) | `phone`、`wechat`、`qq` |
| provider_user_id | VARCHAR(128) | 否 | | | 手机号或第三方用户 ID |
| credential_hash | VARCHAR(255) | 是 | NULL | | 手机号密码哈希；第三方身份为空 |
| verified_at | DATETIME(3) | 是 | NULL | | 验证通过时间 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

### 3.3 `verification_code` 验证码记录

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 记录 ID |
| target_hash | CHAR(64) | 否 | | IDX | 手机号目标哈希 |
| purpose | VARCHAR(24) | 否 | | IDX | `register`、`login`、`reset_password`、`bind`、`deactivate` |
| code_hash | CHAR(64) | 否 | | | 验证码哈希 |
| expires_at | DATETIME(3) | 否 | | IDX | 默认 5 分钟有效 |
| attempts | TINYINT UNSIGNED | 否 | 0 | | 最大 5 次 |
| consumed_at | DATETIME(3) | 是 | NULL | | 使用时间 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |

### 3.4 `auth_session` 登录会话

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 会话 ID |
| user_id | BIGINT UNSIGNED | 否 | | FK，IDX | 用户 ID |
| refresh_token_hash | CHAR(64) | 否 | | UK | 刷新令牌哈希 |
| device_id | VARCHAR(128) | 是 | NULL | | 设备标识 |
| expires_at | DATETIME(3) | 否 | | IDX | 刷新令牌过期时间 |
| revoked_at | DATETIME(3) | 是 | NULL | | 撤销时间 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |

### 3.5 `media_asset` 媒体文件

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 文件 ID |
| owner_user_id | BIGINT UNSIGNED | 否 | | FK，IDX | 所属用户 |
| object_key | VARCHAR(512) | 否 | | UK | 对象存储键 |
| media_type | VARCHAR(16) | 否 | `image` | | 本期固定为图片 |
| mime_type | VARCHAR(64) | 否 | | | JPEG、PNG、WebP |
| size_bytes | INT UNSIGNED | 否 | | | 单张最大 10 MB |
| width | SMALLINT UNSIGNED | 是 | NULL | | 图片宽度 |
| height | SMALLINT UNSIGNED | 是 | NULL | | 图片高度 |
| sha256 | CHAR(64) | 否 | | IDX | 文件指纹 |
| access_status | VARCHAR(16) | 否 | `private` | IDX | `private`、`public`、`deleted` |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| deleted_at | DATETIME(3) | 是 | NULL | | 删除时间 |

### 3.6 `identification_request` 鉴别任务

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 鉴别任务 ID |
| user_id | BIGINT UNSIGNED | 否 | | FK，IDX | 提交用户 |
| status | VARCHAR(16) | 否 | `created` | IDX | 见任务状态枚举 |
| processing_at | DATETIME(3) | 是 | NULL | | 进入 `processing` 的时间，回调超时看门狗的判定基准 |
| description | VARCHAR(2000) | 是 | NULL | | 文字补充描述 |
| feature_json | JSON | 是 | NULL | | 纹理、大小、气味、触感、湿度、味型 |
| material_id | BIGINT UNSIGNED | 是 | NULL | FK | 最终识别药材 |
| idempotency_key | VARCHAR(64) | 否 | | UK(user_id, idempotency_key) | 客户端幂等键 |
| submitted_at | DATETIME(3) | 否 | | IDX | 提交时间 |
| completed_at | DATETIME(3) | 是 | NULL | | 终态时间 |
| failure_code | VARCHAR(32) | 是 | NULL | | 见失败原因编码枚举 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

### 3.7 `identification_request_asset` 鉴别任务图片关联

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| request_id | BIGINT UNSIGNED | 否 | | PK/FK | 鉴别任务 ID |
| asset_id | BIGINT UNSIGNED | 否 | | PK/FK | 图片 ID |
| sort_order | TINYINT UNSIGNED | 否 | | | 展示顺序，从 1 开始 |
| created_at | DATETIME(3) | 否 | | | 关联时间 |

每个任务必须关联 1 至 5 张图片，创建后不可追加或替换。

### 3.8 `identification_result` 鉴别结果

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 结果 ID |
| request_id | BIGINT UNSIGNED | 否 | | UK/FK | 对应鉴别任务 |
| conclusion | VARCHAR(16) | 否 | | | 真伪结论 |
| material_id | BIGINT UNSIGNED | 是 | NULL | FK | 匹配药材 ID |
| material_name | VARCHAR(128) | 是 | NULL | | 展示名称 |
| recognition_confidence | DECIMAL(6,5) | 是 | NULL | | 识别置信度，0 至 1 |
| authenticity_confidence | DECIMAL(6,5) | 是 | NULL | | 真伪置信度，0 至 1 |
| model_name | VARCHAR(64) | 否 | | | 模型名称 |
| model_version | VARCHAR(32) | 否 | | | 模型版本 |
| explanation | TEXT | 是 | NULL | | 面向用户的说明 |
| risk_notice | TEXT | 否 | | | 风险提示 |
| raw_output_json | JSON | 是 | NULL | | 原始模型输出，仅内部可见 |
| created_at | DATETIME(3) | 否 | | | 生成时间 |

### 3.9 `material` 药材主表

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 药材 ID |
| name | VARCHAR(128) | 否 | | UK，IDX | 标准名称 |
| category | VARCHAR(16) | 否 | | IDX | `plant`、`animal`、`mineral` |
| scientific_name | VARCHAR(256) | 是 | NULL | | 学名 |
| status | VARCHAR(16) | 否 | `published` | IDX | `draft`、`published`、`offline` |
| cover_asset_id | BIGINT UNSIGNED | 是 | NULL | FK | 封面图 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

### 3.10 `material_alias` 药材别名

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 别名 ID |
| material_id | BIGINT UNSIGNED | 否 | | FK，IDX | 药材 ID |
| alias | VARCHAR(128) | 否 | | UK(material_id, alias)，IDX | 别名 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |

### 3.11 `material_standard` 药材参考标准

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 标准记录 ID |
| material_id | BIGINT UNSIGNED | 否 | | FK，IDX | 药材 ID |
| source_name | VARCHAR(256) | 否 | | | 标准来源名称 |
| source_version | VARCHAR(64) | 是 | NULL | | 来源版本或发布日期 |
| standard_code | VARCHAR(128) | 是 | NULL | IDX | 标准编号 |
| indicator_json | JSON | 是 | NULL | | 检测指标及参考范围 |
| status | VARCHAR(16) | 否 | `published` | IDX | `draft`、`published`、`offline` |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

鉴别结果中的 `reference_standards` 只返回已发布且与识别药材匹配的标准摘要，不直接暴露内部维护字段。

### 3.12 `material_knowledge` 药材知识

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 记录 ID |
| material_id | BIGINT UNSIGNED | 否 | | UK/FK | 药材 ID |
| efficacy | TEXT | 是 | NULL | | 功效说明 |
| contraindication | TEXT | 是 | NULL | | 注意事项与禁忌 |
| storage_condition | TEXT | 是 | NULL | | 储存条件 |
| source_name | VARCHAR(256) | 是 | NULL | | 内容来源 |
| source_version | VARCHAR(64) | 是 | NULL | | 内容版本 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

### 3.13 `feedback` 投诉与反馈

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| id | BIGINT UNSIGNED | 否 | | PK | 反馈 ID |
| user_id | BIGINT UNSIGNED | 否 | | FK，IDX | 提交用户 |
| type | VARCHAR(24) | 否 | `suggestion` | IDX | `suggestion`、`complaint`、`identification`、`other` |
| content | VARCHAR(2000) | 否 | | | 反馈内容 |
| status | VARCHAR(16) | 否 | `submitted` | IDX | `submitted`、`processing`、`resolved`、`rejected` |
| request_id | BIGINT UNSIGNED | 是 | NULL | FK | 关联鉴别任务 |
| created_at | DATETIME(3) | 否 | | | 创建时间 |
| updated_at | DATETIME(3) | 否 | | | 更新时间 |

### 3.14 `feedback_asset` 反馈图片关联

| 字段 | 类型 | 空 | 默认值 | 约束/索引 | 说明 |
|---|---|:---:|---|---|---|
| feedback_id | BIGINT UNSIGNED | 否 | | PK/FK | 反馈 ID |
| asset_id | BIGINT UNSIGNED | 否 | | PK/FK | 图片 ID |
| created_at | DATETIME(3) | 否 | | | 关联时间 |

## 4. 关系与访问规则

- 一个用户可以拥有多个登录身份、会话、媒体文件、鉴别任务和反馈。
- 一个鉴别任务关联 1 至 5 张图片，最多有一个鉴别结果。
- 鉴别任务、结果、原图和反馈只允许所属用户读取，必须进行资源级鉴权。
- 药材及药材知识为公开只读数据，只返回 `published` 药材。
- `user_account.avatar_asset_id` 与 `media_asset.owner_user_id` 构成循环引用，建表迁移时应在两个表创建完成后添加外键。
- 账号注销后 `user_account.status` 置为 `deleted`，全部会话撤销、登录身份失效，昵称等展示信息脱敏；鉴别记录与反馈按保留策略继续留存（默认长期），不再向用户返回。
- 审计与安全日志写入独立日志通道，业务库不保存审计日志。

## 5. 固定业务参数

验证码为 6 位数字，5 分钟有效，同一手机号 60 秒内最多发送一次且最多验证 5 次。密码长度 8 至 64 位，须同时包含字母和数字。访问令牌有效期 2 小时，刷新令牌有效期 30 天，轮换后旧刷新令牌立即失效。图片只支持 JPEG、PNG、WebP，单张不超过 10 MB。模型回调超时阈值为进入 `processing` 后 120 秒；内部接口签名时间戳容差为 ±300 秒，单次领取任务上限 5 条。鉴别记录 MVP 阶段默认长期保存；用户注销后的数据处理按隐私政策实施。
